rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow authenticated users to upload their own profile pictures
    match /profiles/{userId}/{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if request.auth != null && request.auth.uid == userId;
    }

    // Allow authenticated users to upload daily posting screenshots
    match /daily-postings/{userId}/{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if request.auth != null && request.auth.uid == userId;
    }

    // Allow authenticated users to upload doctor images
    match /doctors/{doctorId}/{allPaths=**} {
      allow read, write: if request.auth != null;
    }

    // Temporary: Allow all authenticated users (for testing)
    // Remove this after confirming the above rules work
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
